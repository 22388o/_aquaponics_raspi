def reporting (current_time_string:str,
               current_date_string:str,
               text_subject:str,
               text_body:str,
               address_from:str,
               address_to:str):
    import email_setup as mail
